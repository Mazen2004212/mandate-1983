# Public, non-secret application environment label.
NEXT_PUBLIC_APP_ENV=development

# Supabase variables will be introduced by the authorized persistence task.
# Do not add real credentials or secrets to this file.

